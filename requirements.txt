# Версии зафиксированы по той же причине, что и версия Claude Code CLI в
# Dockerfile: иначе очередная пересборка молча привозит новые, и бот, который
# работал вчера, ломается на ровном месте. Обновлять осознанно, с проверкой.
aiohttp==3.14.3
python-dotenv==1.2.2
claude-agent-sdk==0.2.134
# certifi — только набор корневых сертификатов, и устаревать ему вредно:
# просроченный корень означает, что перестанет открываться часть интернета.
certifi>=2026.7.22
# Картинки для отправки в ВК: половина сайтов отдаёт webp, а ВК берёт
# фотографией только jpg, png и gif. Заодно ужимает слишком большие.
pillow==12.3.0
